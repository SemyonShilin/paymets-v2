<dl>
  <% @months.each do |month| %>
      <dt><%= link_to month.name_month, new_month_payment_path(month.id) %></dt>
  <% end %>
</dl>


<%= collection_select @payment, :month_id, @months, :id, :name_month, include_blank: false %>
<%= button_to 'Новый платеж', new_month_payment_path() %>
